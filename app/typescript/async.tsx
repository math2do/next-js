// async/await keyword allows us to write completely synchronous looking code, while performing
// asynchronous tasks

export function AsyncMain() {
  // Timers and Intervals are not in javascript, but in browser
  // Cleaning these is very important
  // setTimeout(() => {
  //   console.log("Logging after timeout");
  // }, 2000);
  // setInterval(() => {
  //   console.log("SetInterval print at interval of 3 seconds");
  // }, 3000);
}
